env:
  global:
    - "ARTIFATCS_BUCKET = flashcardsbucket"
    - "ARTIFATCS_REGION = eu-central-1"
    - "ARTIFATCS_KEY = ENV['AWS_ACCESS_KEY_ID']"
    - "ARTIFATCS_SECRET = ENV['AWS_SECRET_ACCESS_KEY']"
language: ruby
rvm:
  - 2.2
gemfile: Gemfile
before_script:
  - psql -c 'create database travis_ci_test;' -U postgres
  - "cp config/database.yml.travis config/database.yml"
  - "cp config/secrets.yml.travis config/secrets.yml"
addons:
  artifacts: true