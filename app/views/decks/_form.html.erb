<%= simple_form_for @deck,
  wrapper: :inline_form,
  html: class: 'form-inline' do |f| %>
  <%= f.error_notification %>

  <%= f.input :name, placeholder: 'Имя колоды' %>

  <%= f.button :submit, id: "add_deck_btn" %>
<% end %>