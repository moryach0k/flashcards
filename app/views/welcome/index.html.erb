<p><%= t("welcome.promotion") %></p>
<p id="notice"><%= notice %></p>
<div class="main_form">
  <div id="translation_form"> 
    <% if @card.nil? %>
      <%= render 'no_cards_to_repeat' %>
    <% else %>
      <%= render 'form' %>
    <% end %>
  </div>
</div>